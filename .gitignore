# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
*.env.local

# IDE
.vscode/
.idea/
*.swp

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Data (large files stay out of git by default)
data/transcripts/
# exports/ are generated daily feature matrices — not committed
exports/
# Block all parquet except the curated sample set in data/samples/
data/**/*.parquet
exports/**/*.parquet
*.parquet
# Sample data (10-ticker 5m subset) — small enough for regular git, no LFS
!data/samples/
!data/samples/**
*.csv.gz

# Model artifacts (binary, re-trainable)
models/

# Node (stray installs)
node_modules/
package.json
pnpm-lock.yaml

# Logs
logs/
*.log

*.key
*.pem
.env
*recovery*
Alpaca*Keys*
Alpaca*

# Scratch diagnostic probes (not deliverables -- ignored, not deleted).
# Explicit list, not a scripts/_*.py glob: scripts/_apply_migrations.py,
# _audit_universe.py, _check_hyp_schema.py, _gather_stats.py,
# _migrate_exp_scores.py, _migration_lib.py are real tracked scripts that
# happen to share the leading-underscore convention.
query_patterns.py
scripts/_probe_schema.py
scripts/_ts_diag.py
scripts/_validate_gaps.py
scripts/_validate_gaps2.py
